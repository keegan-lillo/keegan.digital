[build]
  publish = "public"
  command = "yarn build"

[build.environment]
  NODE_VERSION = "12"
  YARN_FLAGS = "--ignore-optional --frozen-lockfile"

[[plugins]]
  package = "netlify-plugin-gatsby-cache"
